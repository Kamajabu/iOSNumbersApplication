# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NumbersApplication' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NumbersApplication
	pod 'SDWebImage', '~> 4.0'

  target 'NumbersApplicationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NumbersApplicationUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
